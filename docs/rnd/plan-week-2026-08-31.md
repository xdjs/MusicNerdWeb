# Plan — week of Monday 31 August

> **Historical weekly plan, last status recorded September 2.** PR states, launch dates and
> remaining tasks below are not live. See [MEMORY.md](../../MEMORY.md) for engineering state
> and the [meeting index](meetings/README.md) for later decisions.

From the 27 August R&D meeting. Standups start Monday, 10:00 ET. Onboarding
launches **September 4th, 2pm ET**.

No day assignments — this is the list, tackled in roughly this order.

## Where it stands — 2 September

- **Production is migrated.** All 11 (`0011`-`0021`) applied and verified: 8 tables, 23 indexes,
  11 of `0017`'s correctly absent (`0019` replaces them), 2 columns, `mnweb` privileges on all 8.
  Runbook and end-state verifier in `prod-steps/`.
- **Staging carries everything**, including the artist's own profile image in search and the fixed
  share-preview URL (#1196).
- **#1195 (`staging` → `main`) is open and green.** Waiting on Carl's approval and merge. That is
  the last thing between here and production.
- Environment variables verified present in Vercel Production. `WEB_SEARCH_PROVIDER` is absent and
  harmless — it defaults to `"tavily"`, the only implemented provider.

## Next, after the merge

- **The latest-activity section** — agreed at the 9/2 standup, and the next thing to build.
- **Bookmarks.** Interns stubbed the feature out; it resumes once the profile work is on main.
- **User profile redesign.** Tom meeting Wednesday 9:00 ET on UI; SoundCloud's profile structure
  is the reference to study.
- **Dupes prototype** on YouTube playlists, needed by the 11th for his event.

## What the meeting settled

- **Underground artists only.** Big names are a non-goal for now.
- **Interview questions go in the chat**, not email.
- **Contribution mechanics deferred.** Nothing beyond links at this stage;
  artist-curated personas are the differentiator from zero-click.
- **Profile pages must be scrapable by bots.** Carl: heavy client-side
  JavaScript means bots cannot execute it, so there is nothing to scrape. CY's
  goal: LLMs referring to Music Nerd as their source.
- **A domain credibility list**, not just ranking — a list the judge consults.
  High signal like Pitchfork, low signal like indices and scrapers.
- **MNTv after profile claiming.**

## It has to work for Pete first

Nobody else onboards until it works properly for one artist. Testing with other
artists before that just multiplies the same defects across more people.

---

## The ask cannot answer a question about "lately"

This is the real one, and it is bigger than the missing citations.

The ask **never reads the artist's posts**. It reads only the credits and
statements extracted from them. And `posted_at` is stored on every one of those
rows but `getSocialCredits` drops it on the way out, so even the derived
material arrives with no dates attached.

So "what has Pete Rango been up to lately" is structurally unanswerable: 299
posts available, none read, and nothing that is read can be ordered by time.

Fix:

- Carry `postedAt` through `getSocialCredits` and into the prompt.
- Give the ask the recent posts themselves — captions, dates, links — not just
  what was extracted from them months of feed ago. Recency questions want the
  post, not the credit.
- Order what it sees by date so "lately" means something.

**On fetching live from Instagram per question:** not viable. A scrape is one
to five minutes and the ask is a text box someone is waiting at. Freshness
comes from the stored feed being refreshed — the "Look again" button and the
job queue already do that, and the artist can trigger it. If the posts are
stale, that is a refresh problem, not an ask problem.

**Done when** the meeting's own test — "what has Pete Rango been up to lately"
— answers with something actually recent, from a post, with a link to it.

## The three ask failures from the demo

All shipped to staging at 02:44 ET on the 27th and were live for ten hours
before the demo. Not deploy lag.

1. The ask **errored** on "what has Pete Rango been up to lately."
2. **No source pills**, though `citable` numbers the vault, statements,
   collaborators and self-credits.
3. **No hyperlinks** on "who are Pete's collaborators," and that request was
   slow enough to remark on.

Likely one diagnosis, not three: the caption context is built and then not
used, or not built at all. Reproduce on `staging.musicnerd.xyz` with both
meeting queries. The error first — a failure on a normal question is worse than
a missing pill.

## Domain credibility list

Boom Play and Clubhouse, in the shape Carl and CY described: a list the judge
consults while deciding, not an ordering applied afterwards. What exists today
only sorts.

- Three tiers: preferred, unknown, low-signal.
- The tier reaches `judgeSourceRelevance`, so a low-signal domain needs
  stronger evidence to survive.
- One editable list, so adding a domain is a one-line change.

It ranks and gates. A low-signal source that survives still reaches the artist,
who decides.

**Amended 27 August.** Pete, on the Boomplay page in his own vault: "I don't
want boom play anywhere... I don't want any links like that." So there is a
fourth category above the three tiers, and it does delete: a short explicit list
of hosts that never enter the vault at all. The line is authorship, not quality.
Every page on boomplay.com, viberate.com, soundcharts.com and clubhousedb.com is
generated from a scrape or a catalogue feed, so there is no article to read and
nothing the artist said. Ranking such a page low still leaves it on their page.

**Done when** a fresh run on Pete Rango files no Boom Play and no Clubhouse,
and the benchmark still scores 10/12 with zero wrong.

## Bot scrapability

Start with the measurement, because it decides whether this is an afternoon or
a project: fetch an artist page the way a bot does, JavaScript disabled, and
see what is actually in the HTML.

Then: server-render the profile content that matters, publish the knowledge
base as a crawlable page, add structured data so a crawler knows what it is
looking at.

The public knowledge base is **for LLMs and search engines**. Fans need a
better surface than the edit view — separate work, not now.

Not doing yet: making the MCP server public without an auth token. Carl said
track it, not do it.

**Done when** an artist page returns its substance in the initial HTML and the
knowledge base is readable without a login.

## Before September 4th

The job queue has never run with several artists onboarding at once, and every
failure found last week was in that seam. Enqueue several artists together with
several pumps ticking, and check no job is claimed twice, no cursor goes
backwards, nothing completes while unfinished.

And walk one artist end to end in a browser. That has still never happened on
this build.

---

## Parked

- **Interview delivery as a chatbot.** Decided, but the questions already
  generate — this is delivery, not capability.
- **The collaborator feed.** Seeing collaborators' latest activity on your own
  profile; Carl's "we saw you did this interview, should we add it to the
  vault." This is the liveness work and the most interesting thing on the list.
  It needs design.
- **Releases sourced at export time** rather than stored in the document.
- **MNTv**, per the agreed sequencing.
- **Big artists.**

## For standup

- Confirm the ask diagnosis before committing to the rest.
- Boom Play is the test case; name any others already seen.
